services:
  db:
    image: postgres:18
    restart: unless-stopped
    environment:
      POSTGRES_USER: gamecolector
      POSTGRES_PASSWORD: gamecolector
      POSTGRES_DB: gamecolector
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql

volumes:
  db_data:
